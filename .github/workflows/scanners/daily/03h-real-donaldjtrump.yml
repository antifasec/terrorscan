name: Scan real_DonaldJTrump Channel

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  scan:
    uses: ./.github/workflows/templates/scan-only-template.yml
    with:
      channel: real_DonaldJTrump
      data_dir: real_DonaldJTrump
    secrets: inherit
