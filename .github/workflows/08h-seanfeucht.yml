name: Scan seanfeucht Channel

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  scan:
    uses: ./.github/workflows/scan-only-template.yml
    with:
      channel: seanfeucht
      data_dir: seanfeucht
      depth: 5
      max_messages: 999
      max_channels: 999
    secrets: inherit
