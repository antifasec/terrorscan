name: Scan TPUSA Channel

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:
    inputs:
      reset_manifest:
        description: "Reset manifest and start fresh"
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  scan:
    uses: ./.github/workflows/scan-template.yml
    with:
      channel: TPUSA
      data_dir: tpusa
      reset_manifest: ${{ inputs.reset_manifest || false }}
    secrets: inherit