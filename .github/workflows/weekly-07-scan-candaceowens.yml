name: Scan CandaceOwensReal Channel

on:
  schedule:
    - cron: "0 7 * * 0"
  workflow_dispatch:
    inputs:
      reset_manifest:
        description: "Reset manifest and start fresh"
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  scan:
    uses: ./.github/workflows/scan-template.yml
    with:
      channel: CandaceOwensReal
      data_dir: CandaceOwensReal
      reset_manifest: ${{ inputs.reset_manifest || false }}
    secrets: inherit
